global:
  scrape_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "springboot-app"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets:
          - "app:8080"